# PRODIGY_BWD_10
This project is a RESTful Users API implementing CRUD operations using an in-memory data structure. Each user has a UUID, name, email, and age. The API includes input validation, proper HTTP status codes, error handling, and is tested using Postman.
